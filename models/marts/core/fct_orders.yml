version: 2

models:
  - name: fct_orders
    columns:
      - name: order_id
        tags:
          - pk_tag
        tests:
          - unique
          - not_null
      - name: should_not_be_null
        tests:
          - not_null
